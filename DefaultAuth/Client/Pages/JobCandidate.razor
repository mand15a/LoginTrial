@page "/jobcandidate"


<table>
    <thead>
        <tr>
            @for (int i = 0; i < 10; i++)
            {
                <th>Column @(i + 1)</th>
            }
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < 10; i++)
        {
            <tr>
                @for (int j = 0; j < 10; j++)
                {
                    <td>Row @(i + 1), Column @(j + 1)</td>
                }
            </tr>
        }
    </tbody>
</table>

@code {

}
